# notmuch Puppet Module for Boxen

Install notmuch from homebrew.

## Usage

```puppet
include notmuch
```

## Required Puppet Modules

* `boxen`
* `homebrew`

## Development

Write code. Run `script/cibuild` to test it. Check the `script`
directory for other useful tools.
